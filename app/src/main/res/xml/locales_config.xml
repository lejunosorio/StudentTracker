<?xml version="1.0" encoding="utf-8"?>
<!--
    Locales this app ships translations for. Android 13+ reads this to offer a per-app language
    picker in system settings. Add a locale here as soon as its values-<code>/strings.xml exists.
-->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="fil" />
</locale-config>
